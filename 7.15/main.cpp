#include <iostream>

using namespace std;

int main()
{
   const size_t rows=3;
   const size_t columns=5;
   int main()
   {
       array<array<int,columns>,rows>sales1={};
       array<array<int,columns>,rows>sales2={1,2,3};
       array<array<int,columns>,rows>sales3={12,11};
       for(size_t row=0;row<3;++row)
        foe(size_t columns=0;columns<5;++columns)
        salea[row][columns]=0;
       cout<<sales[row][columns];
   }

}
